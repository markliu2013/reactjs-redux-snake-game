import React from 'react';

const ControlStatus = () => (
    <div className="controlButtons">
        <button>Start</button>
        <button>Pause</button>
        <button>Reset</button>
    </div>
);

export default ControlStatus;