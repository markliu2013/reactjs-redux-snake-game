const initialState = {
    gridRowNum: 2,
    gridColNum: 2
};

export default function board(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}